# Core TARS dependencies (server + model)
fastapi==0.110.0
uvicorn==0.30.1
openai==0.28.1
python-dotenv>=1.0.0
requests>=2.31.0
pydantic<2
numpy>=1.26.0

# Optional: testing utilities (if you run tests)
pytest>=8.0.0

# Optional: PC-only audio dependencies for live_voice_chat.py
# Install these only on your PC, not required on Termux.
# You can install them manually on PC with:
#   pip install sounddevice playsound
#sounddevice>=0.4.6
#playsound==1.3.0
